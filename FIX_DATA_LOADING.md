# 数据加载问题修复

## 问题描述

应用启动时出现错误：
```
Failed to load data: Error: HTTP error! status: 403
```

## 问题原因

应用尝试从 GitHub Raw URL 加载数据：
```
https://raw.githubusercontent.com/yuanxin518/cascade-select-for-quicker/main/packages/quicker-tree-select-core/data/data.json
```

由于网络限制或 GitHub API 限制，导致 403 Forbidden 错误。

## 解决方案

### 1. 修改数据源配置

将数据源从远程 GitHub URL 改为本地文件：

**文件**: `packages/quicker-tree-select-webui/src/providers/data-fetcher-provider.ts`

```typescript
// 修改前
const DEFAULT_DATA_URL = 'https://raw.githubusercontent.com/...'

// 修改后
const DEFAULT_DATA_URL = '/data/data.json'
```

### 2. 添加本地数据文件

创建 `public/data/` 目录并复制数据文件：

```bash
mkdir -p packages/quicker-tree-select-webui/public/data
cp packages/quicker-tree-select-core/data/data.json \
   packages/quicker-tree-select-webui/public/data/data.json
```

### 3. 增强示例数据

添加了更丰富的示例数据，包含多个分类：
- 密码相关（项目、个人、工作）
- 工作（项目、文档、会议）
- 学习（编程、语言）
- 生活（购物、健康）
- 娱乐（电影、音乐）

## 验证

### 测试数据文件访问

```bash
curl http://127.0.0.1:5173/data/data.json
```

应该返回 JSON 数据。

### 浏览器测试

1. 访问 http://127.0.0.1:5173/
2. 应该能看到标签选择器
3. 选择标签后应该能看到匹配的数据

## 当前可用标签

- **密码相关** - 项目、个人、工作
- **工作** - 项目、文档、会议
- **学习** - 编程、语言
- **生活** - 购物、健康
- **娱乐** - 电影、音乐

## 使用示例

### 示例 1: 查找工作相关的项目
1. 选择标签: "工作"
2. 再选择标签: "项目"
3. 结果: 显示工作项目列表

### 示例 2: 查找密码
1. 选择标签: "密码相关"
2. 再选择标签: "个人" 或 "项目" 或 "工作"
3. 结果: 显示对应类别的密码信息

### 示例 3: 学习资源
1. 选择标签: "学习"
2. 再选择标签: "编程" 或 "语言"
3. 结果: 显示学习资源列表

## 自定义数据

你可以编辑 `public/data/data.json` 文件来添加自己的数据：

```json
[
  {
    "tags": ["标签1", "标签2"],
    "data": ["数据1", "数据2", "数据3"]
  },
  {
    "tags": ["标签1", "标签3"],
    "data": {
      "键1": "值1",
      "键2": "值2"
    }
  }
]
```

### 数据格式说明

- **tags**: 字符串数组，定义数据项的标签
- **data**: 可以是数组或对象
  - 数组格式: `["item1", "item2"]`
  - 对象格式: `{"key": "value"}`

## 热重载

修改 `public/data/data.json` 后：
- 刷新浏览器即可看到新数据
- 不需要重启服务器

## 下一步

现在数据加载问题已解决，你可以：

1. ✅ 测试标签过滤功能
2. ✅ 添加自己的数据
3. 🔄 集成 SQLite 数据管理（需要后端 API）
4. 🔄 添加数据编辑界面（需要后端 API）

## 相关文件

- 数据源配置: `src/providers/data-fetcher-provider.ts`
- 数据文件: `public/data/data.json`
- 核心逻辑: `quicker-tree-select-core`

---

**修复时间**: 2026-01-29
**状态**: ✅ 已解决
